# Core
flask==3.1.0
flask-cors==5.0.0

# Supabase
supabase==2.10.0

# Auth
PyJWT==2.9.0

# AIsupabase>=2.3.4
google-generativeai
agora-token-builder==0.8.3

# Agora video tokens
# This section now only contains the comment as agora-token-builder was moved.

# Sentiment analysis (optional — falls back to keyword matching if absent)
textblob==0.19.0

# Environment
python-dotenv==1.0.1

# Testing
pytest==8.3.5
pytest-flask==1.3.0
